rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Keep storage private by default; emulator connectivity tests may receive 401/403.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
